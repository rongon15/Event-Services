server:
  port: 8082


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/service_provider
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true